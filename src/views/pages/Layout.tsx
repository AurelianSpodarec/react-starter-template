import CustomRoutes from "routes/CustomRoutes";

function Layout() {
    return (
        <div>

            <header>
                Header
            </header>

                <CustomRoutes />

            <footer>
                Footer
            </footer>

        </div>
    )
}

export default Layout;